[build-system]
requires = ["setuptools>=80"]
build-backend = "setuptools.build_meta"

[project]
name = "xconv2"
version = "0.3.0b2"
description = "High-performance data viewer and simple data converter"
requires-python = ">=3.12"
dependencies = [
    "cfdm @ git+https://github.com/davidhassell/cfdm.git@pyfive-netcdf",
    "cf-python @ git+https://github.com/davidhassell/cf-python.git@kerchunk-read",
    "cf-plot @git+https://github.com/bnlawrence/cf-plot.git@main",
    "p5rem @git+https://github.com/NCAS-CMS/p5rem.git@main",
    "esmpy",
    "fsspec",
    "numpy",
    "pandas",
    "paramiko",
    "prov",
    "pyfive",
    "PySide6",
    "rdflib",
    "s3fs",
    "setuptools>80",  # cf-plot imports distutils
    "superqt",
    "umfive>=0.2.2",
    "zarr",
    "pystac-client",
    "Pillow"
]

[project.optional-dependencies]
test = [
    "docker",
    "minio",
    "pytest",
]

[project.gui-scripts]
# This creates an 'xconv2' command that launches your GUI
xconv2 = "xconv2.gui:main"

[project.scripts]
# This creates a 'cf-worker' command for the backend process
cf-worker = "xconv2.worker:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["xconv2*"]

[tool.setuptools.package-data]
xconv2 = ["assets/*.svg", "assets/*.png"]

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = [
    "ignore:distutils Version classes are deprecated. Use packaging.version instead.:DeprecationWarning:cfplot.cfplot",
    "ignore:'maxsplit' is passed as positional argument:DeprecationWarning:cf.functions",
]